#include <iostream>
#include <numeric>
#include <vector>
#include <map>
using namespace std;

void solve() {
	int x, y;
	cin >> x >> y;
	/*
	y % 2 = 0, x = 1 => res = y * y;
	x % 2 = 1, y = 1 => res = x * x; 
	*/
}

int main() {
	int n;
	cin >> n;
	while (n--)
    	solve();
    return 0;
}